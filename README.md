## Useful Commands:

```bash
# Initializes terraform providers, backend and working directory
terraform init

# Shows the changes to terraform's managed state
terraform plan

# Apply the changes outlined by plan
terraform apply
# (Optional) Skip the prompt
terraform apply -auto-approve
```
